#!/bin/sh

# Pre-commit hook: Run linting, type checking, and architecture validation
# This ensures code quality before commits reach the repository

echo "🔍 Running pre-commit checks..."

# Run lint-staged (ESLint + Prettier on staged files)
echo "📝 Checking staged files with lint-staged..."
npx lint-staged
if [ $? -ne 0 ]; then
    echo "❌ lint-staged failed. Please fix the issues above."
    exit 1
fi

# Run TypeScript type check
echo "🔎 Running TypeScript type check..."
npm run typecheck
if [ $? -ne 0 ]; then
    echo "❌ TypeScript type check failed. Please fix type errors."
    exit 1
fi

# Run architecture validation
echo "🏗️ Validating DDD architecture..."
npm run validate:layers
if [ $? -ne 0 ]; then
    echo "❌ Architecture validation failed. Check layer dependencies."
    exit 1
fi

echo "✅ All pre-commit checks passed!"
